A little PHP script to run the C++ binary. Mainly exists so that I could have a play with setting up an Apache server and getting it to run some code.

Wherever this ends up being used, you'll also need the train_bin binary (or a simlink thereto).
